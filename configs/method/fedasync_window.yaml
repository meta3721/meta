name: fedasync_window
description: FedAsync-Window: alpha proportional to raw count * exp(-kappa_tau * staleness)
uses_design_ratio: false
uses_obs_correction: false
uses_use_correction: false
uses_instant_calibration: false
uses_debt: false
uses_reference: false
uses_variance: false
uses_staleness: true
