# Official reproducibility environment for RAVEN-MCS V2.3.
# Constitution requires Python 3.11.
name: raven-mcs
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - -r requirements-lock.txt
      - -e .
