# E2 Dataset Scope Recommendation

**Recommendation (pending teacher authorization): D1 SensorScope as the primary E2 matrix; D2 (U-Air) and D3 (NSW Traffic) are reproducibility extensions.**

This follows the existing E2 stub's SensorScope scope while preserving the design brief's three-dataset coverage. Traffic remains conditional on its documented quality gate; it must not be silently replaced. Recommendation is not formal authorization.
